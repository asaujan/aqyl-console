[theme]
primaryColor = "#2E7D32"
base = "light"

[server]
maxUploadSize = 50

[runner]
# Не прерывать выполняющийся скрипт входящими событиями виджетов:
# иначе массовый репуш обрывается посреди цикла (rerun ставится в очередь
# и выполняется после завершения текущего прогона).
fastReruns = false
